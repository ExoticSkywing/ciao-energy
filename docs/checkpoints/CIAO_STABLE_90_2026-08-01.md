# Ciao Energy Stable Checkpoint 32

## Identity

- Internal checkpoint: `CIAO_STABLE_90_2026-08-01`
- Runtime cache version: `stable-rollback-32`
- Human assessment: approximately 90% of the source mobile experience.
- Public URL at capture: `http://45.8.22.65:44118`

## Restore artifact

- Archive: `docs/checkpoints/ciao-energy-stable-rollback-32.tar.gz`
- SHA-256 file: `docs/checkpoints/ciao-energy-stable-rollback-32.sha256`
- SHA-256: `54d3a883dcb94876f5decf89391d74a0cd362f93a26b1a172070ae16bab0ea0b`
- Archive excludes regenerable/heavy directories: `node_modules`, `.next`, `.venv`, `.playwright-browsers`, and `docs/checkpoints` itself.

## Human-approved baseline scope

Preserve these behaviors when restoring:

- Loader logo and percentage intro.
- Interactive Three.js Hero cans.
- Horizontal can carousel and vertical scroll gesture routing.
- Mobile Profile pre-stage title and description at source-like scale.
- Four Benefit copy states without overlap.
- Benefit icon highlight synchronized with spotlight state.
- Badge strike-through and full body copy.
- Benefit copy exit/staggered entry animation in both directions.

## Deliberately excluded

- Argument fluid-loop background playback is disabled. A later attempt caused broad regressions and was rolled back.

## Verification at capture

- `npm run check`: PASS.
- Hero gestures: horizontal changed carousel without vertical scroll; vertical changed scroll without carousel change.
- Profile title test: PASS.
- Benefit copy motion sequence `1 -> 2 -> 3 -> 4 -> 3 -> 2 -> 1`: PASS 7.
- Public root: HTTP 200.

## Restore procedure

From the project parent directory, stop the running preview, move the damaged working tree aside, extract the archive into a clean `ciao-energy` directory, run `npm install` if dependencies are absent, run `npm run check`, then start on port 44118. Verify that `src/app/page.tsx` contains `stable-rollback-32` before declaring restoration successful.
